package fr.inria.coming.core.entities.interfaces;

import fr.inria.coming.core.engine.Analyzer;

/** analizes a commit */
public interface CommitAnalyzer extends Analyzer<Commit> {

	// ResultRevision<Commit> analyze(Commit commit);

}
